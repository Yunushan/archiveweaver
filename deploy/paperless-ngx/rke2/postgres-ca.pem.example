# Stage the approved PostgreSQL server CA certificate as postgres-ca.pem.
# Its PEM bytes must be protected in the staged Kustomize tree and match
# the CA that signed the certificate for PAPERLESS_DBHOST.
# Do not copy this comment-only template as the real certificate.
